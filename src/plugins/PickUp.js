"use strict";

(function () {
    if (typeof Breinify !== 'object') {
        return;
    }

    // create the AlertMe module
    var PickUp = {


    };

    // bind the module
    Breinify.plugins.pickUp = PickUp;
})();